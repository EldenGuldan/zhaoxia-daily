{
  "name": "zhaoxia-daily-root",
  "private": true,
  "scripts": {
    "build": "cd crawler && python3 main.py; cd ../web && npm install && npm run build",
    "dev": "cd web && npm run dev",
    "crawl": "cd crawler && python3 main.py"
  }
}